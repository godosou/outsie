<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-feature
        android:name="android.software.companion_device_setup"
        android:required="true" />

    <uses-permission android:name="android.permission.REQUEST_OBSERVE_COMPANION_DEVICE_PRESENCE" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <application>
        <service
            android:name="ai.repose.mobile.unlock.companion.ReposeCompanionDeviceService"
            android:directBootAware="false"
            android:enabled="@bool/repose_companion_presence_service_enabled"
            android:exported="true"
            android:permission="android.permission.BIND_COMPANION_DEVICE_SERVICE">
            <intent-filter>
                <action android:name="android.companion.CompanionDeviceService" />
            </intent-filter>
            <property
                android:name="android.companion.PROPERTY_PRIMARY_COMPANION_DEVICE_SERVICE"
                android:value="true" />
        </service>
    </application>
</manifest>
